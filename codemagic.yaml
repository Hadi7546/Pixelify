workflows:
  native-android:
    name: Native Android
    max_build_duration: 120
    instance_type: mac_mini_m1
    scripts:
      - name: Build Android release
        script:
          gradlew assembleRelease

    artifacts:
      - app/build/outputs/**/*.zip
    publishing:
      email:
        recipients:
          - hazard7546@gmail.com
        notify:
          success: true
          failure: false
